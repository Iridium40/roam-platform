# Include optional dependencies (needed for Rollup native modules)
include=optional

# Ensure native modules are properly resolved
node-linker=hoisted

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Force installation of native modules
audit=false
